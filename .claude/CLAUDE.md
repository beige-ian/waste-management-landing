# Waste Management Landing Page - 개발 가이드

## 프로젝트 개요

**프로젝트명**: 커버링 빌딩 - 쓰레기 처리 솔루션 랜딩 페이지
**기술 스택**: React 18 + TypeScript + Tailwind CSS v4 + Vite
**Git Repository**: https://github.com/beige-ian/waste-management-landing

---

## 🏗️ 프로젝트 구조

```
src/
├── components/
│   ├── ProblemSolution.tsx    # 메인 섹션 (문제 → 해결책 → 결과)
│   ├── RecyclingChart.tsx      # 재활용률 비교 차트
│   ├── Hero.tsx                # 히어로 섹션
│   ├── Navbar.tsx              # 네비게이션
│   └── [기타 컴포넌트]
├── App.tsx                     # 메인 앱
├── index.css                   # 글로벌 스타일
└── main.tsx                    # 진입점

public/
├── truck.png                   # 원스탑 서비스 이미지
├── 영수증.png                   # 비용 절감 이미지
└── [기타 에셋]
```

---

## 📌 주요 컴포넌트

### ProblemSolution.tsx (라인 1-400)
**목적**: 건물주의 문제점과 해결책을 시각화하는 핵심 섹션

**구조**:
1. **건물주의 현실** (라인 50-143)
   - 4개의 문제 카드 (청소, 수거거부, 민원, 비용)
   - 애니메이션 효과 적용

2. **건물주님의 삶이 달라집니다** (라인 145-217)
   - 3개의 결과 카드 (10시간, -80%, 10만원)
   - 카운팅 애니메이션 (IntersectionObserver)

3. **Figma 섹션 1** (라인 219-300) - 원스탑 서비스
   - 인라인 스타일 (Figma MCP에서 추출)
   - truck.png 이미지 포함

4. **Figma 섹션 2** (라인 302-387) - 비용 절감
   - 인라인 스타일 (Figma MCP에서 추출)
   - 영수증.png 이미지 포함

5. **RecyclingChart** (라인 389-391)
   - 재활용률 비교 (27% vs 90%)

---

## 🎨 디자인 토큰 (Figma에서 추출)

### 색상
- Primary: `#3385ff` (파랑)
- Text Heavy: `#46474c` (짙은 회색)
- Text Strong: `#5a5c63` (회색)
- Background: `#eaf2fe` (밝은 파랑)

### 타이포그래피
- Title: Pretendard Bold 36px
- Body: Pretendard Regular 16px
- Label: Pretendard Bold 14px

### 간격/반경
- Padding: 56px
- Border Radius: 40px (큰 섹션), 6px (칩)

---

## 🛠️ 개발 워크플로우

### 파일 수정 시
```bash
/setup    # 처음 한 번만
/start    # 개발 서버 시작
# 파일 수정...
/sync     # 다른 Mac 코드 받기 (필요시)
/commit   # 변경사항 저장
```

### 커밋 메시지 규칙
```
Fix: 버그 수정
Feature: 새로운 기능
Style: UI/스타일 변경
Refactor: 코드 정리
Docs: 문서 작성
```

---

## 📝 수정 시 주의사항

### Figma 섹션 (라인 219-387)
- ⚠️ 인라인 스타일 사용 (Figma MCP에서 자동 추출됨)
- 이미지 경로: `/truck.png`, `/영수증.png` (public 폴더)
- Tailwind가 아닌 인라인 스타일로 유지

### ProblemSolution 섹션 (라인 50-217)
- ✅ Tailwind CSS 사용 가능
- 애니메이션: `animate-fade-in-up` (custom)
- 카운팅: `displayValues` 상태 사용

### 이미지 추가 시
- public 폴더에 저장
- src="/filename.png" 형식 사용
- 한글 파일명 주의 (URL 인코딩)

---

## 🚀 배포

```bash
# 빌드
npm run build

# 결과
dist/  # 정적 파일 생성
```

---

## 📞 트러블슈팅

### git push 실패
- SSH 키 확인: `ls ~/.ssh/id_ed25519`
- GitHub 공개키 확인: Settings → SSH Keys

### npm install 실패
- node_modules 삭제 후 재설치:
```bash
rm -rf node_modules
npm install
```

### 개발 서버 충돌
```bash
lsof -i :5173  # 포트 확인
kill -9 <PID>  # 프로세스 종료
npm run dev    # 재시작
```

---

## 💡 다른 Mac에서 시작할 때

1. `/setup` 실행
2. 파일 수정
3. `/commit` 실행
4. (다른 Mac에서) `/sync` 실행

---

**Last Updated**: 2025-11-22
**GitHub**: https://github.com/beige-ian/waste-management-landing
